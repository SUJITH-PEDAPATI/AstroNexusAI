// Static demo data removed — graph now loads live from /graph endpoint.
import type { GraphNodeT, GraphLinkT } from '@/types'
export const GRAPH_NODES: (GraphNodeT & { color: string })[] = []
export const GRAPH_LINKS: GraphLinkT[] = []
